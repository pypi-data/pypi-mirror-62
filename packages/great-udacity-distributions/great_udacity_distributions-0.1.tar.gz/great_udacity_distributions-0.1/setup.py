from setuptools import setup

setup(name='great_udacity_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['great_udacity_distributions'],
      zip_safe=False)
