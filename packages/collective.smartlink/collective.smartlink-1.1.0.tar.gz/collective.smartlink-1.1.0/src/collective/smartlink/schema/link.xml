<model xmlns="http://namespaces.plone.org/supermodel/schema"
       xmlns:form="http://namespaces.plone.org/supermodel/form"
       xmlns:i18n="http://xml.zope.org/namespaces/i18n"
       i18n:domain="collective.smartlink">
    <schema>
      <field name="remoteUrl" type="zope.schema.TextLine">
        <default></default>
        <description i18n:translate="label_remoteurl_description">
          The link is used almost verbatim, relative links become absolute and the strings "${navigation_root_url}" and "${portal_url}" get replaced with the real navigation_root_url or portal_url. If in doubt which one to use, please use navigation_root_url.
        </description>
        <title>URL</title>
      </field>
    </schema>
</model>
