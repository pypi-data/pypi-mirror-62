from setuptools import setup

setup(name='distributions_quicktools',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_quicktools'],
      zip_safe=False)
