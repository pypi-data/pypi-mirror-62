import default

input, button {
    cursor: pointer;
}
table.grid {
    border-width: 0px;
    padding: 2px;
    border-collapse: collapse;
    border-spacing: 0px;
    margin: 0px;
    width: 720px;
}
.segno {
    font-size: smaller;
}
.transcription {
    border: 1px solid #ddd;
    width: 500px;
    height: 26px;
    cursor: pointer;
}
.controls {
    border: 1px solid #ddd;
}
.recctls {
    width: 80px;
}
.instructions {
    font-size: smaller;
}
.thinbutton {
    padding: 0px;
}
:disabled {
    cursor: default;
}
